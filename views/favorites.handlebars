<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
<!-- Navbar -->
<nav class="navbar navbar-expand-lg" style="background-color: #693668;">
    <a class="navbar-brand" href="#"></a>
    <img src="../Logo.parodyPokemon.png" width="30" height="30" alt="">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item active">
          <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Featured</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">WishList</a>
        </li>
        <li class="nav-item">
          <a class="nav-link disabled" href="#"></a>
        </li>
      </ul>
    </div>
  </nav>
<h1 style="text-align: center;">Favorites</h1>

<ul style="text-align: center;">
  {{#each games}}
    <li>
      <p>
        {{this.id}}. {{this.game}}

        <button data-gameid="{{this.id}}" class="delgame">Delete</button>
      </p>
    </li>
  {{/each}}
</ul>

<h2>Add a game</h2>
<form id="addgame" class="button-size">
	<textarea type="text" name="game"></textarea>
	<button type="submit">Save game</button>
</form>

<h2>Update a game</h2>
<form id="updategame" class="button-size">
  <input type="text" name="id" placeholder="id">
	<textarea type="text" name="game" placeholder="what do you want to update this game title to?"></textarea>
	<button type="submit">Update game</button>
</form>

<script type="text/javascript">
  $(".delgame").on("click", function(event) {
    var id = $(this).data("gameid");
    // Send the DELETE request.
    $.ajax("/api/games/" + id, {
      type: "DELETE"
    }).then(
      function() {
        console.log("deleted id ", id);
        // Reload the page to get the updated list
        location.reload();
      }
    );
  });
  $("#addgame").on("submit", function(event) {
    // Make sure to preventDefault on a submit event.
    event.preventDefault();
    var newGame = {
      game: $("#addgame [name=game]").val().trim()
    };
    // Send the POST request.
    $.ajax("/api/games", {
      type: "POST",
      data: newGame
    }).then(
      function() {
        console.log("added new game");
        // Reload the page to get the updated list
        location.reload();
      }
    );
  });
  $("#updategame").on("submit", function(event) {
    // Make sure to preventDefault on a submit event.
    event.preventDefault();
    var id = $("[name=id]").val().trim();
    var updatedGame = {
      game: $("#updategame [name=game]").val().trim()
    };
    // Send the PUT request.
    $.ajax("/api/games/" + id, {
      type: "PUT",
      data: updatedGame
    }).then(
      function() {
        console.log("updated id ", id);
        // Reload the page to get the updated list
        location.reload();
      }
    );
  });
</script>